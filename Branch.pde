class Branch {
  float x, y;
  float angle;
  float length;
  float noiseOffset;

  Branch(float x, float y, float angle, float length, float noiseOffset) {
    this.x = x;
    this.y = y;
    this.angle = angle;
    this.length = length;
    this.noiseOffset = noiseOffset;
  }
}
